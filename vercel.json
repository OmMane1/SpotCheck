{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/health", "destination": "/api/index" },
    { "source": "/recommendations", "destination": "/api/index" }
  ]
}
